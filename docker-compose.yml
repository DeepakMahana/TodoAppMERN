version: '3'

services:

    server:
        restart: always
        build: 
           dockerfile: Dockerfile
           context: ./todo-api
        volumes:
            - /app/node_modules
            - ./todo-api:/app
        environment: 
            - LOGS_INFO=true
            - MONGO_URI_MLAB=mongodb+srv://dcoder:dcoder123@cluster0.ryzqm.mongodb.net/todoapp
            - SERVICE_PORT=5000
            - JWT_SALT=qwertrtytf
            - BYCRYPT_SALT=10

    nginx:
        restart: always
        build: 
            dockerfile: Dockerfile
            context: ./nginx
        ports: 
            - '8080:80'

    client:
        build: 
            dockerfile: Dockerfile
            context: ./todo-client
        volumes:
            - /app/node_modules
            - ./todo-client:/app